/* 
interface Node {
	visited: boolean;
	children: Node[];
} 
*/

import Queue from '../../Queues/Queue.solution';

export default (visitCallback) => {
	return (search = (root) => {
		//implement here
	});
};
